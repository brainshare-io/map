// TODO set up the CLI to install dependencies and make a nice environment for
// local dev

// TODO can publishing to npm be optional? Would make downstream builds way
// faster of course. Maybe github OK for private and npm for public? Or just do
// heavy caching?

export default function Map({ data }: { data: any }) {
  return <>Map</>;
}
